// TODO: Write a simple Makefile to compile a single C source file named application.c into an executable named "app". This will be in a new file in the same directory called "Makefile".

#include <stdio.h>

int main() {
    printf("Hello, Makefile!\n");
    return 0;
}
