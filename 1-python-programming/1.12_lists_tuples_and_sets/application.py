#!/bin/python
# Python Lists Tuples And Sets - Application

# TODO:  Use *list splicing* (not list comprehension or for loops) to return only the elements at odd indeces in the passed-in list
# e.g.: INPUT=[8, 'b', 9, 'c']; RETURN=['b','c']
def lists_splice_odds(input :list):
    pass


# TODO: Return a new set, which only contains the items that are present in both sets A and B (an *intersection* in Set Theory)
# e.g.: A = {922, "howdy", "cherry", "google"}; B = {"google", "gigem", 922};  RETURN {922, "google"}
def sets_intersect_sets(A: set, B: set):
    pass


# TODO: Return all 4 input parameters at once using a tuple
def tuples_ret_all(one, two, three, four):
    pass
